using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PsychicExtraFunctionality : MonoBehaviour
{

    [NonSerialized]
    public bool buffing;

    // Start is called before the first frame update
    void Start()
    {
        buffing = false; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
